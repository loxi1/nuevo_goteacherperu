---
const { theme, mobileHidden } = Astro.props

const socialNetworks = [
	{
		name: 'Twitter',
		icon: '/socialmedia/twitter.svg',
		href: 'https://twitter.com/loxi1'
	},
	{
		name: 'Tiktok',
		icon: '/socialmedia/tiktok.svg',
		href: 'https://www.tiktok.com/@brianfer30'
	},
	{
		name: 'Instagram',
		icon: '/socialmedia/instagram.svg',
		href: 'https://www.instagram.com/loxi1'
	},
	{
		name: 'Youtube',
		icon: '/socialmedia/youtube.svg',
		href: 'https://www.youtube.com/@anibal.cayetano@gmail.com'
	}
]
---

<div class={`gap-4 justify-center ${mobileHidden ? 'hidden md:flex' : 'flex'}`}>
	{
		socialNetworks.map(({ href, icon, name }) => (
			<a
				href={href}
				target='_blank'
				rel='noopener norefferer'
				class={`w-11 h-11 flex items-center justify-center rounded-full transition-all duration-500 ${
					theme === 'dark' ? 'bg-white' : 'bg-transparent hover:bg-white'
				} [&>img]:hover:scale-125 [&>img]:hover:invert-0 [&>img]:hover:brightness-100`}
			>
				<img
					class={`w-5 transition-all duration-500 ${
						theme === 'dark' ? 'invert-0' : 'invert-[100]'
					} brightness-0 aspect-square`}
					src={icon}
					alt={`${name} Logo`}
					width="20"
					height="20"
					loading='lazy'
					decoding='async'
					fetchpriority='low'
				/>
			</a>
		))
	}
</div>
